server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql:///cloud-user?useSSL=false
    username: root
    pasword:
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb_
