server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql:///cloud-order?useSSL=false
    username: root
    pasword:
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  global-config:
    db-config:
      table-prefix: tb_
